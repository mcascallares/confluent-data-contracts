---
version: '3.9'
services:
  tester:
    hostname: tester
    image: confluentinc/cp-schema-registry:${TAG}
    volumes:
      - ./run.sh:/run.sh
    # does not start Schema Registry service, just use the container to execute the test
    entrypoint: /bin/sh 
    command: "echo 'Tester Container Started'"
    depends_on:
      schema-registry:
        condition: service_healthy